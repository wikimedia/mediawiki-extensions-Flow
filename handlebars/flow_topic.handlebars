<div class="flow-topic {{#unless isPreview}}flow-load-interactive{{/unless}} {{#if isModerated}}flow-topic-moderated flow-topic-collapsed-invert{{/if}}" id="flow-topic-{{postId}}" data-flow-id="{{postId}}" data-flow-load-handler="topicElement">
	{{>flow_topic_titlebar}}

	{{#each replies}}
		{{#eachPost @root this}}
			<!-- eachPost topic -->
			{{post @root this}}
		{{/eachPost}}
	{{/each}}

	{{#unless isPreview}}
		{{> flow_reply_form}}
	{{/unless}}
</div>
