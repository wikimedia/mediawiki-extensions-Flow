/*!
 * Runs Flow code, using methods in FlowUI.
 */

( function ( $ ) {
	/*
	 * Now do stuff
	 * @todo not like this
	 */
	$( document ).ready( function () {
		mw.flow.initComponent( $( '.flow-component' ) );
	} );
}( jQuery ) );

