<form method="POST" action="{{moderationAction actions @root.submitted.moderationState}}">
	{{> flow_errors}}
	<input type="hidden" name="wpEditToken" value="{{@root.editToken}}" />
	<textarea name="topic_reason"
	          placeholder="{{moderationMessage "flow-moderation-placeholder-" @root.submitted.moderationState "-topic"}}">
		{{~#if @root.submitted.reason~}}
			{{~@root.submitted.reason~}}
		{{~/if~}}
	</textarea>
	<div class="flow-form-actions flow-form-collapsible">
		<button class="flow-ui-button flow-ui-constructive"
				data-flow-interactive-handler="apiRequest"
				data-flow-api-handler="moderateTopic">
			{{~moderationMessage "flow-moderation-confirm-" @root.submitted.moderationState "-topic"~}}
		</button>
		<a class="flow-ui-button flow-ui-destructive flow-ui-quiet"
		   href="{{links.topic.url}}"
		   title="{{l10n "flow-cancel"}}"
		   data-flow-interactive-handler="cancelForm">
			{{~l10n "flow-cancel"~}}
		</a>
	</div>
</form>
