<div class="flow-board">
	{{> flow_errors}}
	{{!-- There is only one post, but the output format matches multi-post output --}}
	{{#each roots}}
		{{#eachPost @root this}}
			{{> flow_moderate_post}}
			{{> flow_post}}
		{{/eachPost}}
	{{/each}}
</div>
